{#
/**
 * @file
 * Theme override for features block.
 * Icon-less horizontal feature strip.
 */
#}
<section class="feature-strip"{{ attributes }}>
  <div class="feature">
    <h4>{{ 'Built for communities'|t }}</h4>
    <p>{{ 'Local organisers, grassroots events, and independent voices come first.'|t }}</p>
  </div>
  <div class="feature">
    <h4>{{ 'No platform fees to list'|t }}</h4>
    <p>{{ 'Publish your event without upfront costs. Optional upgrades only when you need them.'|t }}</p>
  </div>
  <div class="feature">
    <h4>{{ 'Inclusive by design'|t }}</h4>
    <p>{{ 'Accessibility, diversity, and respect are defaults, not add-ons.'|t }}</p>
  </div>
</section>
