{#
/**
 * @file
 * Default unformatted view template.
 * Wraps rows in canonical event grid for consistent layout across all event listings.
 */
#}
{% if rows %}
  <div{{ attributes.addClass('mel-event-grid') }}>
    {% for row in rows %}
      {{ row.content }}
    {% endfor %}
  </div>
{% endif %}
