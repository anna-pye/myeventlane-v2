{#
/**
 * @file
 * Theme override - outputs event cards without row wrappers.
 * Grid wrapper is applied at page template level.
 */
#}
{% if rows %}
  {% for row in rows %}
    {{ row.content }}
  {% endfor %}
{% endif %}
