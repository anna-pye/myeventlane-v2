{#
/**
 * @file
 * Template override for Taxonomy Term view (event listings).
 *
 * Canonical event grid wrapper - no additional markup.
 */
#}
{% if rows %}
  <div class="mel-event-grid">
    {% for row in rows %}
      {{- row.content -}}
    {% endfor %}
  </div>
{% endif %}
