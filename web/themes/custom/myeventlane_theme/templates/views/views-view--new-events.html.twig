{#
/**
 * @file
 * Theme override for the New Events view.
 * Wraps rows in canonical event grid for consistent layout.
 */
#}
{% if rows %}
  <div{{ attributes.addClass('mel-event-grid') }}>
    {{ rows }}
  </div>
{% endif %}
{% if pager %}
  {{ pager }}
{% endif %}
{% if empty %}
  <div class="view-empty">
    <p>No new events at this time.</p>
  </div>
{% endif %}













