{#
/**
 * @file
 * Theme override for the New Events view.
 * Outputs with proper wrapper structure for grid CSS.
 */
#}
<div class="view view-new-events">
  <div class="view-content">
    {{ rows }}
  </div>
  {% if pager %}
    {{ pager }}
  {% endif %}
  {% if empty %}
    <div class="view-empty">
      <p>No new events at this time.</p>
    </div>
  {% endif %}
</div>



