{#
/**
 * @file
 * Theme override for unformatted views rows - Featured Events.
 * Outputs rows directly - grid wrapper is applied at page template level.
 */
#}
{% if rows %}
  {% for row in rows %}
    {{ row.content }}
  {% endfor %}
{% endif %}





