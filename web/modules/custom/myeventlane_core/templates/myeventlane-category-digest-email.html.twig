{#
/**
 * @file
 * Email template for weekly category digest.
 *
 * Available variables:
 * - user: The user receiving the digest.
 * - events_by_category: Array of events grouped by category.
 */
#}
<style>
  body {
    font-family: 'Nunito', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    background: #fef5ec;
    padding: 24px;
    margin: 0;
  }
  .wrap {
    background: white;
    border-radius: 16px;
    padding: 32px;
    max-width: 600px;
    margin: 0 auto;
    box-shadow: 0 2px 8px rgba(0,0,0,0.1);
  }
  h1 {
    font-size: 28px;
    font-weight: 700;
    color: #293241;
    margin: 0 0 16px 0;
  }
  .intro {
    color: #666;
    font-size: 16px;
    line-height: 1.6;
    margin-bottom: 32px;
  }
  .category-section {
    margin-bottom: 40px;
    padding-bottom: 32px;
    border-bottom: 2px solid #f0f0f0;
  }
  .category-section:last-child {
    border-bottom: none;
  }
  .category-title {
    font-size: 22px;
    font-weight: 700;
    color: #293241;
    margin: 0 0 20px 0;
  }
  .event {
    background: #fafafa;
    border-radius: 12px;
    padding: 20px;
    margin-bottom: 16px;
    border-left: 4px solid #ff6f61;
  }
  .event.featured {
    background: linear-gradient(135deg, #fff5f3 0%, #fafafa 100%);
    border-left-color: #ff6f61;
  }
  .event-title {
    font-size: 20px;
    font-weight: 700;
    color: #293241;
    margin: 0 0 12px 0;
  }
  .event-title a {
    color: #293241;
    text-decoration: none;
  }
  .event-title a:hover {
    color: #ff6f61;
  }
  .event-meta {
    color: #666;
    font-size: 14px;
    margin-bottom: 16px;
    line-height: 1.8;
  }
  .event-meta-item {
    margin-bottom: 4px;
  }
  .featured-badge {
    display: inline-block;
    background: linear-gradient(135deg, #ff6f61 0%, #ff8c7a 100%);
    color: white;
    padding: 4px 12px;
    border-radius: 999px;
    font-size: 12px;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.5px;
    margin-bottom: 12px;
  }
  .event-actions {
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }
  .btn {
    display: inline-block;
    padding: 10px 20px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    font-size: 14px;
  }
  .btn-primary {
    background: #ff6f61;
    color: white;
  }
  .btn-primary:hover {
    background: #ff5a4a;
  }
  .btn-link {
    color: #ff6f61;
    text-decoration: underline;
  }
  .footer {
    margin-top: 32px;
    padding-top: 24px;
    border-top: 1px solid #e0e0e0;
    text-align: center;
    color: #999;
    font-size: 12px;
  }
</style>

<div class="wrap">
  <h1>📅 Weekly Event Digest</h1>
  <p class="intro">
    Hi {{ user.name.value }},<br>
    Here are new events this week in the categories you follow:
  </p>

  {% for category_data in events_by_category %}
    <div class="category-section">
      <h2 class="category-title">{{ category_data.category.label }}</h2>

      {% for event in category_data.events %}
        <div class="event{% if event.is_boosted %} featured{% endif %}">
          {% if event.is_boosted %}
            <span class="featured-badge">⭐ Featured</span>
          {% endif %}
          <h3 class="event-title">
            <a href="{{ event.url }}">{{ event.title }}</a>
          </h3>
          <div class="event-meta">
            {% if event.start_date %}
              <div class="event-meta-item">
                <strong>📅 Date:</strong> {{ event.start_date }}{% if event.start_time %} at {{ event.start_time }}{% endif %}
              </div>
            {% endif %}
            {% if event.venue %}
              <div class="event-meta-item">
                <strong>📍 Venue:</strong> {{ event.venue }}
              </div>
            {% endif %}
          </div>
          <div class="event-actions">
            <a href="{{ event.url }}" class="btn btn-primary">View Event</a>
            {% if event.ics_url %}
              <a href="{{ event.ics_url }}" class="btn btn-link">Add to Calendar</a>
            {% endif %}
          </div>
        </div>
      {% endfor %}
    </div>
  {% endfor %}

  <div class="footer">
    <p>You're receiving this because you follow these categories on MyEventLane.</p>
    <p><a href="{{ url('<front>', {'absolute': true}) }}">Visit MyEventLane</a> | <a href="{{ url('myeventlane_core.my_categories', {'absolute': true}) }}">Manage Categories</a></p>
  </div>
</div>




















