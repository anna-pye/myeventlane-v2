<!DOCTYPE html>
<html>
<head>
  <style>
    body {
      font-family: Nunito, sans-serif;
      padding: 24px;
    }
    .ticket {
      border: 2px solid #f26d5b;
      border-radius: 16px;
      padding: 24px;
    }
    .code {
      font-size: 28px;
      font-weight: bold;
      color: #f26d5b;
    }
  </style>
</head>
<body>
  <div class="ticket">
    <h1>{{ event_title }}</h1>
    <p>{% if event_start %}{{ event_start|date("d M Y, g:ia") }}{% endif %}</p>
    {% if location %}
      <p>{{ location }}</p>
    {% endif %}

    <div class="code">
      Ticket Code: {{ code }}
    </div>

    <p>Ticket Holder: {{ holder }}</p>
  </div>
</body>
</html>