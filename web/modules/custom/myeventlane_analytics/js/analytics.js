/**
 * @file
 * JavaScript for MyEventLane Analytics.
 */

(function (Drupal, drupalSettings) {
  'use strict';

  /**
   * Initialises analytics dashboard.
   */
  Drupal.behaviors.myeventlaneAnalytics = {
    attach: function (context, settings) {
      // Analytics-specific JavaScript can be added here.
      // For example, chart initialisation if using Chart.js or similar.
    }
  };

})(Drupal, drupalSettings);



